using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Game : MonoBehaviour {

    public GameObject pref;
    AbstractFactory factory;

    private void Start()
    {
    //    factory = new AbstractFactory(pref);
    }
}
